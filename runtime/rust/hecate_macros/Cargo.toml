[package]
name = "hecate_macros"
version = "0.1.0"
edition = "2021"
description = "Proc macros for Hecate Rust apps"
license = "MIT"

[lib]
proc-macro = true
path = "src/lib.rs"

[dependencies]
proc-macro2 = "1"
quote = "1"
syn = { version = "2", features = ["full"] }
