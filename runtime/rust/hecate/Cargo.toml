[package]
name = "hecate"
version = "0.1.0"
edition = "2021"
description = "Hecate runtime + proc macro entrypoint crate"
license = "MIT"

[lib]
path = "src/lib.rs"

[dependencies]
hecate_runtime = { path = "../hecate_runtime" }
hecate_macros = { path = "../hecate_macros" }
