default_syscall_cycles = 500
io_cycles_per_byte = 20

[memory]
kind = "Pages"
page_size = 4096

[latency]
l1 = 3
l2 = 11
l3 = 50
load = 125
store = 1

[syscall_cycles]
# Per-syscall cycle costs.  Keys are decimal or 0x-prefixed hex syscall numbers.
# Example:
64 = 500 # write
93 = 0   # exit
